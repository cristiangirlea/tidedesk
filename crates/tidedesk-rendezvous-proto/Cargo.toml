[package]
name = "tidedesk-rendezvous-proto"
description = "Wire format of the TideDesk rendezvous service, which introduces viewers to hosts and never carries sessions."
version.workspace = true
edition.workspace = true
license-file.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
postcard.workspace = true
ring.workspace = true
serde.workspace = true
tokio = { workspace = true, optional = true }

[features]
# A minimal in-memory service, for tests of hosts and viewers.
test-service = ["dep:tokio"]

[dev-dependencies]
rcgen.workspace = true
